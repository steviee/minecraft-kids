# =================================================================
# MCK Suite Environment Configuration
# =================================================================

# -----------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------
PORT=3000
NODE_ENV=development
JWT_SECRET=your-secret-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=24h
DATABASE_PATH=./data/mck-suite.db
DOCKER_SOCKET=/var/run/docker.sock

# -----------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------
VITE_API_URL=http://api.mck.local

# -----------------------------------------------------------------
# Minecraft Server Configuration
# -----------------------------------------------------------------
# RCON password for all Minecraft servers
RCON_PASSWORD=minecraft-rcon-password-change-this

# Default Minecraft version
MINECRAFT_VERSION=1.20.4

# -----------------------------------------------------------------
# Domain Configuration
# -----------------------------------------------------------------
# Local development domains
BASE_DOMAIN=mck.local
MC_SUBDOMAIN=mc.mck.local

# Production domains (for reference)
# BASE_DOMAIN=minecraft-kids.de
# MC_SUBDOMAIN=mc.minecraft-kids.de

# -----------------------------------------------------------------
# Docker Network Configuration
# -----------------------------------------------------------------
# Docker network name (must match docker-compose.yml)
DOCKER_NETWORK_NAME=mck-network

# Docker network subnet (default: 172.18.0.0/16)
# DOCKER_NETWORK_SUBNET=172.18.0.0/16

# -----------------------------------------------------------------
# Traefik Configuration
# -----------------------------------------------------------------
# Traefik dashboard port (default: 8080)
TRAEFIK_DASHBOARD_PORT=8080

# -----------------------------------------------------------------
# Unbound DNS Configuration
# -----------------------------------------------------------------
# DNS server port (use 5353 to avoid conflicts with host DNS)
DNS_PORT=5353

# -----------------------------------------------------------------
# Optional: Production Configuration
# -----------------------------------------------------------------
# Uncomment and configure for production deployment

# SSL/TLS Configuration
# ACME_EMAIL=admin@minecraft-kids.de
# TRAEFIK_ACME_STORAGE=/letsencrypt/acme.json

# Database backup location
# BACKUP_PATH=/backups

# Log level (debug, info, warn, error)
# LOG_LEVEL=info
